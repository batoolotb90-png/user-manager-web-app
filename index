<?php require_once 'config.php'; ?>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Manager</title>
    <style>
        /* ─── Reset & Base ─── */
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f0f4f8;
            color: #333;
            min-height: 100vh;
            padding: 40px 20px;
        }

        /* ─── Container ─── */
        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        /* ─── Header ─── */
        h1 {
            text-align: center;
            font-size: 1.8rem;
            color: #2d3748;
            margin-bottom: 30px;
        }

        /* ─── Form Card ─── */
        .form-card {
            background: #fff;
            border-radius: 12px;
            padding: 24px 28px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            margin-bottom: 30px;
        }

        .form-row {
            display: flex;
            align-items: center;
            gap: 12px;
            flex-wrap: wrap;
        }

        .form-row label {
            font-weight: 600;
            white-space: nowrap;
        }

        .form-row input[type="text"],
        .form-row input[type="number"] {
            border: 1px solid #cbd5e0;
            border-radius: 8px;
            padding: 8px 14px;
            font-size: 0.95rem;
            outline: none;
            transition: border-color .2s;
        }

        .form-row input[type="text"]   { flex: 1; min-width: 140px; }
        .form-row input[type="number"] { width: 90px; }

        .form-row input:focus { border-color: #667eea; }

        .btn-submit {
            background: #667eea;
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 9px 22px;
            font-size: 0.95rem;
            cursor: pointer;
            transition: background .2s;
            white-space: nowrap;
        }
        .btn-submit:hover { background: #5a67d8; }

        /* ─── Feedback Message ─── */
        #message {
            margin-top: 12px;
            font-size: 0.88rem;
            min-height: 18px;
        }
        #message.success { color: #38a169; }
        #message.error   { color: #e53e3e; }

        /* ─── Table Card ─── */
        .table-card {
            background: #fff;
            border-radius: 12px;
            padding: 24px 28px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }

        .table-card h2 {
            font-size: 1.1rem;
            color: #4a5568;
            margin-bottom: 16px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead tr { background: #667eea; color: #fff; }
        thead th { padding: 11px 14px; text-align: center; font-weight: 600; }

        tbody tr { transition: background .15s; }
        tbody tr:nth-child(even) { background: #f7fafc; }
        tbody tr:hover           { background: #edf2ff; }

        tbody td {
            padding: 10px 14px;
            text-align: center;
            border-bottom: 1px solid #e2e8f0;
        }

        /* ─── Status Badge ─── */
        .badge {
            display: inline-block;
            padding: 2px 10px;
            border-radius: 999px;
            font-size: 0.82rem;
            font-weight: 600;
        }
        .badge-0 { background: #fed7d7; color: #c53030; }
        .badge-1 { background: #c6f6d5; color: #276749; }

        /* ─── Toggle Button ─── */
        .btn-toggle {
            background: #fff;
            border: 1.5px solid #667eea;
            color: #667eea;
            border-radius: 8px;
            padding: 5px 14px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all .2s;
        }
        .btn-toggle:hover {
            background: #667eea;
            color: #fff;
        }

        /* ─── Empty State ─── */
        .empty { text-align: center; color: #a0aec0; padding: 20px 0; }

        /* ─── Loading Spinner ─── */
        .spinner {
            display: inline-block;
            width: 16px; height: 16px;
            border: 2px solid #667eea;
            border-top-color: transparent;
            border-radius: 50%;
            animation: spin .6s linear infinite;
            vertical-align: middle;
            margin-left: 6px;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
    </style>
</head>
<body>
<div class="container">
    <h1>👤 User Manager</h1>

    <!-- ─── Form ─── -->
    <div class="form-card">
        <div class="form-row">
            <label for="nameInput">Name:</label>
            <input type="text"   id="nameInput" placeholder="Enter name" />

            <label for="ageInput">Age:</label>
            <input type="number" id="ageInput"  placeholder="Age" min="1" max="150" />

            <button class="btn-submit" onclick="addUser()">Submit</button>
        </div>
        <div id="message"></div>
    </div>

    <!-- ─── Table ─── -->
    <div class="table-card">
        <h2>📋 Records</h2>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Age</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="tableBody">
                <?php
                $result = $conn->query("SELECT id, name, age, status FROM users ORDER BY id ASC");
                if ($result && $result->num_rows > 0):
                    while ($row = $result->fetch_assoc()):
                        $s = (int)$row['status'];
                ?>
                <tr id="row-<?= $row['id'] ?>">
                    <td><?= $row['id'] ?></td>
                    <td><?= htmlspecialchars($row['name']) ?></td>
                    <td><?= $row['age'] ?></td>
                    <td><span class="badge badge-<?= $s ?>"><?= $s ?></span></td>
                    <td>
                        <button class="btn-toggle" onclick="toggleStatus(<?= $row['id'] ?>)">
                            Toggle
                        </button>
                    </td>
                </tr>
                <?php endwhile; else: ?>
                <tr id="empty-row">
                    <td colspan="5" class="empty">No records yet. Add one above!</td>
                </tr>
                <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>

<script>
/* ─── Add User ─── */
async function addUser() {
    const name = document.getElementById('nameInput').value.trim();
    const age  = document.getElementById('ageInput').value.trim();
    const msg  = document.getElementById('message');

    // Client-side validation
    if (!name) { showMsg('Please enter a name.', 'error'); return; }
    if (!age || age <= 0 || age > 150) { showMsg('Please enter a valid age.', 'error'); return; }

    showMsg('<span class="spinner"></span> Saving...', '');

    try {
        const body = new URLSearchParams({ name, age });
        const res  = await fetch('add_user.php', { method: 'POST', body });
        const data = await res.json();

        if (data.success) {
            showMsg('✅ User added!', 'success');
            document.getElementById('nameInput').value = '';
            document.getElementById('ageInput').value  = '';
            appendRow(data.id, data.name, data.age, data.status);
        } else {
            showMsg('❌ ' + data.message, 'error');
        }
    } catch (e) {
        showMsg('❌ Network error.', 'error');
    }
}

/* ─── Append new row without page reload ─── */
function appendRow(id, name, age, status) {
    const tbody = document.getElementById('tableBody');

    // Remove empty-state row if present
    const empty = document.getElementById('empty-row');
    if (empty) empty.remove();

    const tr = document.createElement('tr');
    tr.id = 'row-' + id;
    tr.innerHTML = `
        <td>${id}</td>
        <td>${escapeHtml(name)}</td>
        <td>${age}</td>
        <td><span class="badge badge-${status}">${status}</span></td>
        <td><button class="btn-toggle" onclick="toggleStatus(${id})">Toggle</button></td>
    `;
    tbody.appendChild(tr);
}

/* ─── Toggle Status ─── */
async function toggleStatus(id) {
    try {
        const body = new URLSearchParams({ id });
        const res  = await fetch('toggle_status.php', { method: 'POST', body });
        const data = await res.json();

        if (data.success) {
            const row    = document.getElementById('row-' + id);
            const badge  = row.querySelector('.badge');
            const newSt  = data.new_status;

            // Update badge without reload
            badge.textContent  = newSt;
            badge.className    = 'badge badge-' + newSt;
        } else {
            alert('Toggle failed: ' + data.message);
        }
    } catch (e) {
        alert('Network error.');
    }
}

/* ─── Helpers ─── */
function showMsg(html, type) {
    const el = document.getElementById('message');
    el.innerHTML   = html;
    el.className   = type;
}

function escapeHtml(text) {
    const d = document.createElement('div');
    d.appendChild(document.createTextNode(text));
    return d.innerHTML;
}

// Submit on Enter key
document.addEventListener('keydown', e => {
    if (e.key === 'Enter') addUser();
});
</script>
</body>
</html>